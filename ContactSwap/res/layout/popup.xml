<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_height="fill_parent"
  android:orientation="vertical"
  android:minWidth="310dp"
  android:id="@+id/MainLinearLayout"
  android:layout_width="fill_parent">
  <include
    layout="@layout/popup_header"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" />
  <include
    layout="@layout/popup_divider" />
  <ScrollView
    android:layout_width="fill_parent"
    android:layout_height="0dip"
    android:id="@+id/MessageScrollView"
    android:visibility="gone"
    android:layout_weight="1"
    android:paddingBottom="10sp"
    android:paddingTop="6sp">
    <TextView
      android:id="@+id/MessageTextView"
      android:autoLink="all"
      android:textAppearance="?android:attr/textAppearanceMedium"
      android:textColor="?android:attr/textColorPrimaryDisableOnly"
      android:layout_height="wrap_content"
      android:layout_width="fill_parent"
      android:gravity="center"
      android:paddingRight="10sp"
      android:paddingLeft="10sp" android:text="Contact Swap"/>
  </ScrollView>
  <LinearLayout
    android:id="@+id/ButtonLinearLayout"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:padding="2sp"
    android:background="#333">
    <Button
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:layout_width="fill_parent" android:text="Accept" android:id="@+id/btnAccept"/>
    <Button
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:layout_width="fill_parent" android:text="Decline" android:id="@+id/btnDecline"/>
    
  </LinearLayout>
</LinearLayout>