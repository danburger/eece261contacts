<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/FromLinearLayout"
  android:layout_width="fill_parent"
  android:layout_height="wrap_content"
  android:layout_gravity="left">
  <ImageView
    android:src="@android:drawable/ic_dialog_info"
    android:id="@+id/FromImageView"
    android:layout_width="wrap_content"
    android:layout_gravity="center"
    android:scaleType="center"
    android:layout_height="wrap_content" android:padding="5sp"/>
  <LinearLayout
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_vertical"
    android:id="@+id/FromTextLinearLayout" android:padding="5sp">
    <TextView
      android:id="@+id/FromTextView"
      android:layout_width="fill_parent"
      android:gravity="center_vertical"
      android:autoLink="phone"
      android:textAppearance="?android:attr/textAppearanceLarge"
      android:maxLines="2"
      android:layout_height="wrap_content"
      android:text="Contact Swap"
      android:ellipsize="end" android:paddingBottom="2sp" android:textColor="#F00"/>
    <TextView
      android:textAppearance="?android:attr/textAppearanceSmall"
      android:textColor="?android:attr/textColorPrimaryDisableOnly"
      android:text="Test"
      android:layout_width="fill_parent"
      android:layout_height="wrap_content" android:paddingTop="2sp" android:id="@+id/txtRequest"/>
  </LinearLayout>
</LinearLayout>